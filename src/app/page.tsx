import { DashboardPage } from "@/features/dashboard/dashboard-page";

/**
 * Dashboard route (SSOT Section 2.2 — DashboardPage). Phase 2 content.
 */
export default function Page() {
  return <DashboardPage />;
}
